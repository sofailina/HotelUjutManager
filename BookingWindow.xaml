<Window x:Class="HotelUjutManager.BookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelUjutManager"
        mc:Ignorable="d"
        Title="BookingWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Новое бронирование" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <Label Content="Гость:"/>
                    <ComboBox x:Name="CmbGuests" Height="25"/>

                    <Label Content="Номер комнаты:" Margin="0,10,0,0"/>
                    <ComboBox x:Name="CmbRooms" Height="25"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <Label Content="Дата заезда:"/>
                    <DatePicker x:Name="DpCheckIn" Height="25"/>

                    <Label Content="Дата выезда:" Margin="0,10,0,0"/>
                    <DatePicker x:Name="DpCheckOut" Height="25"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <Label Content="Статус:"/>
                    <ComboBox x:Name="CmbStatus" Height="25">
                        <ComboBoxItem Content="Подтверждено" IsSelected="True"/>
                        <ComboBoxItem Content="Ожидание"/>
                        <ComboBoxItem Content="Отменено"/>
                        <ComboBoxItem Content="Завершено"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="BtnAdd" Content="Добавить" Width="100" Height="30" Margin="5" Click="BtnAdd_Click"/>
            <Button x:Name="BtnSave" Content="Сохранить" Width="100" Height="30" Margin="5" Click="BtnSave_Click"/>
            <Button x:Name="BtnDelete" Content="Удалить" Width="100" Height="30" Margin="5" Click="BtnDelete_Click"/>
            <Button x:Name="BtnClear" Content="Очистить" Width="100" Height="30" Margin="5" Click="BtnClear_Click"/>
        </StackPanel>

        <DataGrid x:Name="DgBookings" Grid.Row="2" Margin="10" 
                  AutoGenerateColumns="True" 
                  SelectionChanged="DgBookings_SelectionChanged"
                  SelectionMode="Single"
                  IsReadOnly="True">
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="BtnRefresh" Content="Обновить" Width="100" Height="30" Margin="5" Click="BtnRefresh_Click"/>
            <Button x:Name="BtnClose" Content="Закрыть" Width="100" Height="30" Margin="5" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
